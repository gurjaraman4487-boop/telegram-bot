# Premium Video Bot

Telegram bot for selling video plans with Cashfree.

## Run Locally

1. Install Python 3.11+
2. Install packages: `pip install -r requirements.txt`
3. Edit `bot.py` and put your `TOKEN`, `ADMIN_ID`, `BOT_USERNAME`, Cashfree keys.
4. Run: `python bot.py`
